# Docker Port Mappings - CHANGE THESE for custom external ports
DOCKER_FRONTEND_PORT=3010             # External port for Frontend (in browser)
DOCKER_BACKEND_PORT=3011              # External port for Backend (for API access)

# Frontend Ports - Automatically use Docker ports
REACT_APP_FRONTEND_DEV_PORT=${DOCKER_FRONTEND_PORT}    # Frontend Development Port
REACT_APP_FRONTEND_PROD_PORT=80                       # Nginx Port in container (usually 80)

# Backend Ports - Automatically use Docker ports
REACT_APP_BACKEND_PORT=${DOCKER_BACKEND_PORT}         # Backend Port for API
PORT=${DOCKER_BACKEND_PORT}                           # Express Server Port

# AI Configuration
AI_API_URL=https://api.deepseek.com/v1
AI_MODEL=deepseek-chat
AI_API_KEY=your-api-key-here

# AI Parameters
AI_MAX_TOKENS=3000
AI_TEMPERATURE=0.2
AI_TOP_P=0.9
AI_FREQUENCY_PENALTY=0
AI_PRESENCE_PENALTY=0
AI_TIMEOUT=30000
AI_SEED=42
AI_MAX_RETRIES=3
AI_RETRY_DELAY=1000

# Contract Enforcement
CONTRACT_MAX_ATTEMPTS=2
CONTRACT_ENABLE_RETRY=true

# Logging
LOG_AI_REQUESTS=false

# Server Configuration
NODE_ENV=production
PORT=3011
CORS_ORIGIN=http://localhost:3010    # Frontend URL for CORS (adjust if port changed)

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=10

# ======================================
# PORT CONFIGURATION GUIDE
# ======================================
# To use different ports:
# 1. Change the values above (e.g. to 8080/8081)
# 2. Save the file
# 3. Run: docker compose down && docker compose build --no-cache && docker compose up -d
# 4. Done! The app now runs on your custom ports
